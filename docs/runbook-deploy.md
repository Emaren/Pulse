# Deploy Runbook

1. Pull repo.
2. Install API dependencies from `apps/api` and restart `pulse-api`.
3. Install worker dependencies from `apps/worker` and restart `pulse-worker`.
4. Install web dependencies/build from `apps/web` and restart `pulse-web`.
5. Reload Nginx.
