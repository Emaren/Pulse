export default function AccountsPage() {
  return (
    <section className="panel">
      <h2>Accounts</h2>
      <p style={{ marginBottom: 8 }}>Connect and manage X/Facebook publisher accounts.</p>
      <small>OAuth UI flow can be added next; API connect endpoints are already scaffolded.</small>
    </section>
  );
}
